pipeline {
    agent any
    stages {
        stage("Clone Git Repository") {
            steps {
                git(
                    url: "https://github.com/richzhang/webpage-template.git",
                    branch: "master",
                    changelog: true,
                    poll: true
                )
            }
        }
    }
}
